programa grafica
var ancho : int = 40
var largo : int = 60
var doble : int = 2
var area : int
var perimetro : int

{
    area = ancho * largo
    ancho = ancho * doble
    largo = largo * doble
    area = ancho * largo
    perimetro = ancho + ancho + largo + largo
}
