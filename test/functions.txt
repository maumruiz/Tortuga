programa fuciones

func poligono(n: int, m: float) {
  var lados : int = 5
  var vertices : float = m
  repetir(n) {
    lados = lados + 1
    vertices = vertices + 2
  }
}

  var algo : float = 6
  var n : int = 3
  mientras(n < 14) {
    poligono(2, algo, 25)
    n = n+1
  }
